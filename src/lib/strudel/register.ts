import '@strudel/mini';
import '@strudel/tonal';
// WebAudio instruments/effects (so s("sine"), filters, etc. exist)
import '@strudel/webaudio';

// Ensure the module isn't elided by the bundler
export {};
